const theme = {
  appMaxWidth: '1200px',
  colors: {
    lightGray: '#707070',
  },
  fontWeights: {
    light: 300,
    bold: 900,
  },
};

type themeType = typeof theme;

export { theme };
export type { themeType };
